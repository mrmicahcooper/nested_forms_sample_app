-#app/views/orders/index.html.haml

%h1 Orders
= link_to "New order", new_order_path
%ul
  - orders.each do |order| #iterater through all the orders
    %li
      = link_to order_label(order), order_path(order)
      |
      = link_to 'edit', edit_order_path(order)

